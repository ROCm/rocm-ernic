# Published by ci/report/publish-perf.py on the nightly run.
# history.jsonl accumulates one JSON object per run.
# badge-rdma.json and badge-tcp.json are shields.io endpoint
# badges regenerated each run; README.md links to them via the
# GitHub Pages build (see docs/conf.py html_static_path).
